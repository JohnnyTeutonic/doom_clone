#include "utils.h"

// Since most utility functions are inline or template-based,
// this file is mostly for non-inline implementations.
// Currently, there are no non-inline implementations needed. 